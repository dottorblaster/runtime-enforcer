apiVersion: batch/v1
kind: CronJob
metadata:
  name: opensuse-cronjob
  namespace: default
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          terminationGracePeriodSeconds: 1
          containers:
          - name: opensuse
            image: registry.opensuse.org/opensuse/bci/bci-ci:latest
            command: ["bash", "-c", "sleep 5; ls"]
          restartPolicy: Never
